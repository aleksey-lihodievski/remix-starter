import { styled } from '~/themes';

const Container = styled('div', {
  display: 'inherit',
  width: '100%',
  maxWidth: '1200px',
  margin: '0 auto',
  padding: '0 1rem',
});

export default Container;
