export {};
// import { selector } from 'recoil';

// import { themeState } from './atom';

// export const getThemeState = selector({
//   key: 'getThemeState', // unique ID (with respect to other atoms/selectors)
//   get: ({ get }) => {
//     return get(themeState);
//   },
//   set: ({ set }, newValue) => {
//     return set(themeState, newValue);
//   },
// });
